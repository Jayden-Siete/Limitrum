{
  "name": "@limitrum/example-mistral-tool-call",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "pnpm --filter @limitrum/db build && pnpm --filter @limitrum/sdk build",
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@limitrum/db": "workspace:*",
    "@limitrum/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "latest",
    "tsx": "latest",
    "typescript": "latest"
  }
}
