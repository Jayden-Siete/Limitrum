{
  "name": "@limitrum/example-yolo-agent",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "pnpm --filter @limitrum/db build && pnpm --filter @limitrum/sdk build",
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@limitrum/sdk": "workspace:*",
    "openai": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "tsx": "latest",
    "typescript": "latest"
  }
}
